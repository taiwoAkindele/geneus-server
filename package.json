{
  "name": "geneus-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#shared": "./shared/src/index.ts"
  },
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "invite": "node scripts/create-invite.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "node scripts/migrate.ts",
    "couch:up": "docker compose --profile legacy up -d couchdb",
    "sync:design": "node scripts/sync-design-docs.ts"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "nano": "^10.1.4",
    "postgres": "^3.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.7.2"
  }
}
