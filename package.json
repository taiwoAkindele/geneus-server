{
  "name": "geneus-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#shared": "./shared/src/index.ts"
  },
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "invite": "node scripts/create-invite.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "test:sync": "node --test tests/powersync.integration.ts",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "sync:up": "docker compose up -d postgres powersync",
    "sync:down": "docker compose stop powersync",
    "db:migrate": "node scripts/migrate.ts",
    "sync:config": "node scripts/generate-sync-config.ts"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "postgres": "^3.4.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.7.2"
  }
}
