{
  "name": "geneus-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#shared": "./shared/src/index.ts"
  },
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "invite": "node scripts/create-invite.ts",
    "typecheck": "tsc --noEmit",
    "couch:up": "docker compose up -d couchdb",
    "couch:down": "docker compose down"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "nano": "^10.1.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.7.2"
  }
}
